{
  "name": "spec-gen-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "concurrently \"pnpm --filter @spec-gen/client dev\" \"pnpm --filter @spec-gen/api dev\"",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.10.0"
  }
}
